springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /api-docs
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=nechto
    username: postgres
    password: qwerty
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
